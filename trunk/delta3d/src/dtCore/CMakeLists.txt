
# FIXME: For OS X, need flag for Framework or dylib
IF (WIN32)
   ADD_DEFINITIONS(-D DT_CORE_LIBRARY)
ENDIF

SET(LIB_NAME     dtCore)

SET(HEADER_PATH ${DELTA3D_SOURCE_DIR}/inc)
SET(LIB_PUBLIC_HEADERS
    ${HEADER_PATH}/*.h
)

ADD_LIBRARY(${LIB_NAME}
    ${LIB_PUBLIC_HEADERS}
    *.cpp
)

TARGET_LINK_LIBRARIES(${LIB_NAME} ${OpenGL} ${MATH_LIBRARY} ${OSG} )

INCLUDE(ModuleInstall OPTIONAL)
