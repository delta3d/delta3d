Import('*')

if env['foundLibs'][env['extLibs']['rbody']] != 'notFound':
   srcs = env.SGlob( '*.cpp' )
   deps = [ 'dtCore', 'dtABC', 'dtChar', 'dtUtil' ]

   env.BuildProgram( 'testCharacters', srcs, deps )
