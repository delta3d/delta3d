Import('*')

dtAbcSrcs = [   'application.cpp',
                'baseabc.cpp',
                'weather.cpp',
                'widget.cpp' ]

env = env.Copy()
#env['LIBPATH'] = env['LIBPATH'] + [env['prefix']+'/src/dtCore']
#print env['LIBPATH']

dtAbcLib = env.SharedLibrary( 'dtabc', dtAbcSrcs, LIBS=[ 'dtcore', 'osgDB', 'plibsg', 'plibul', 'Producer', 'tinyxml' ] )

# TODO: install headers
env.Install( '$prefix/lib', dtAbcLib )
