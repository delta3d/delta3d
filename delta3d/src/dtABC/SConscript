Import('*')

srcs = env.SGlob('*.cpp')

deps = [ 'dtCore', 'dtAI', 'dtDAL', 'fltk', 'dtUtil', 'isense', 'OpenThreads', 'osg', 'osgDB',
         'osgParticle', 'osgText', 'osgUtil', 'osgSim', 'osgFX',
         'ul', 'js', 'Producer', 'xerces-c', 'opengl', 'User32', 'rbody', 'cal3d' ]

defines = []
if env['OS'] == 'windows':
   defines = ['DT_ABC_LIBRARY']       

env.BuildLib( 'dtABC', srcs, deps, defines, prefixHeader = 'inc/prefix/dtcoreprefix.h')
