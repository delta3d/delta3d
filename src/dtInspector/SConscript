Import('*')

srcs = env.SGlob( '*.cpp' )
deps = [ 'dtCore', 'dtABC', 'fltk', 'OpenThreads', 'osg', 'osgDB',
         'osgUtil', 'osgText', 'osgGL2', 'osgParticle', 'osgSim',
         'osgFX', 'Carbon',
         'User32', 'Gdi32', 'shell32', 'Advapi32', 'ole32' ]

defines = []
if env['OS'] == 'windows':
   defines = ['DT_INSPECTOR_LIBRARY']
                    
env.BuildLib( 'dtInspector', srcs, deps, defines )
