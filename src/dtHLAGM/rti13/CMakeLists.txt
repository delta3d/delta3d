
if (RTI_INCLUDE_DIR)

SET(LIB_NAME     dtHLAGM_rti13)

SET(DTHLAGM_RTI13_LIB_BUILT YES CACHE STRING "Whether the hla1.3 plugin for dtHLAGM was built" FORCE)

INCLUDE_DIRECTORIES(${RTI_INCLUDE_DIR})

file(GLOB LIB_PUBLIC_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.h")

if (USE_CERTI)
   ADD_DEFINITIONS(-DCERTI_WORKAROUND)
endif()

SET(LIB_SOURCES
rti13ambassador.cpp
rti13region.cpp
rti13util.cpp
)

set(LIB_EXTERNAL_DEPS
   OSGSIM_LIBRARY
   OPENTHREADS_LIBRARY
)

set(LIB_DEPS
   ${DTUTIL_LIBRARY}
   ${DTGAME_LIBRARY}
   ${DTHLAGM_LIBRARY}
   ${RTI_LIBRARIES}
)

DELTA3D_ADD_LIBRARY(${LIB_NAME} NONE SHARED)

IF (WIN32)
   TARGET_LINK_LIBRARIES(${LIB_NAME} wsock32)
ENDIF (WIN32)

IF (MSVC_IDE)
  SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /wd4290") #ignore the C4290 warning
ENDIF (MSVC_IDE)

endif (RTI_INCLUDE_DIR)
